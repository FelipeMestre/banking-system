[pytest]
testpaths = flink/account-service/tests flink/card-service/tests openbankapi/tests
python_files = test_*.py
markers =
    redis_integration: requires a real Redis (docker compose up redis -d); opt-in via -m redis_integration
addopts = -m "not redis_integration"
