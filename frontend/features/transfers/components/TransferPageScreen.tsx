"use client";

/**
 * Stub for Unit 1 routing — satisfies next build while the full workspace
 * (panel, aside, overlay, modal) lands in later units. Keeps the routing
 * layer thin (app → features) and preserves URL /transfer inside dashboard
 * shell.
 */
export function TransferPageScreen() {
  return (
    <div className="flex min-h-[400px] items-center justify-center">
      <p className="text-neutral-600">Transfer workspace loading…</p>
    </div>
  );
}
